{{ ansible_managed | comment }}
# Environment for the caddy service. Managed by the caddy Ansible role via
# caddy_environment. Referenced by the systemd drop-in as EnvironmentFile.
{% for key, value in caddy_environment | dictsort %}
{{ key }}={{ value }}
{% endfor %}
