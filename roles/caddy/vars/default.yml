---
# Fallback for distros without an OS-family file. The package name is still the
# real one so an unsupported target fails on a missing package, not a missing
# variable. There is no official-repo automation for unknown distros: set
# caddy_use_official_repo=false and preconfigure a repo yourself.
caddy_package_name: caddy
