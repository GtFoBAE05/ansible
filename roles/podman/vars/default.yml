---
# Fallback for OS families without a dedicated vars file. Package names follow
# upstream naming and may need overriding on exotic distributions.
podman_package_name: podman
podman_pasta_package: passt
podman_slirp4netns_package: slirp4netns
podman_fuse_overlayfs_package: fuse-overlayfs
podman_uidmap_package: shadow-utils

podman_extra_packages: []
